[package]
name = "slot-probe"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../../common" }
bip324 = { git = "https://github.com/rust-bitcoin/bip324", package = "bip324", features = ["tokio"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
